/*
  Warnings:

  - You are about to drop the column `timeFormat` on the `BookingSettings` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE "BookingSettings" DROP COLUMN "timeFormat";
